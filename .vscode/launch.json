{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pixel Editor",
            "type": "node",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/launcher/index.js",
            "args": [
                "images",
                "--error",
                "--log"
            ]
        }
    ]
}